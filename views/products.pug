extends layout

block content
  html
    head
      title E-commerce Product List
      meta(charset="UTF-8")
      meta(name="viewport", content="width=device-width, initial-scale=1.0")
      script(src="/javascripts/add_to_cart.js") 
      link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
      style.
        .product-card {
          border: 1px solid #ddd;
          border-radius: 5px;
          padding: 15px;
          margin-bottom: 20px;
          box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .product-image {
          margin-bottom: 10px;
          text-align: center;
        }

        .product-image img {
          max-width: 100%;
          height: auto;
          border-radius: 5px;
        }
        .quantity-wrapper {
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          margin-top: 10px;
        }
        .quantity-wrapper button {
          background-color: #f8f9fa;
          border: 1px solid #ced4da;
          color: #333;
          border-radius: 50%;
          width: 35px;
          height: 35px;
          font-size: 20px;
          line-height: 1;
          transition: background-color 0.2s ease;
          display: flex;
          align-items: center;
          justify-content: center;
        }

        .quantity-wrapper button:hover {
          background-color: #e2e6ea;
        }

        .quantity-wrapper input {
          width: 50px;
          text-align: center;
          border: 1px solid #ced4da;
          border-radius: 5px;
          height: 35px;
        }


    body
    - var rolename = role || 'Customer'
      if rolename == 'Super Admin' || rolename == 'Inventory Manager'
        a(href="/products/create", class="btn btn-success") Add New Product

      
      .container.mt-5
        h1.text-center.mb-4 Product Catalog
        .row
          each product in products
            .col-md-4
              .product-card
                .product-image 
                    img(src="/images/chocu.jpg", alt="product", class="img-fluid")
                h5= product.name
                p.text-muted= product.batch_No
                p.font-weight-bold KES #{product.price}

                //- Add to Cart Form
                form(action="orders/add/to/cart", method="POST")
                  input(type="hidden", name="_csrf", value=csrfToken)
                  input(type="hidden", name="product_token", value=product.token)
                  input(type="hidden", name="productName", value=product.name)
                  input(type="hidden", name="batchNo", value=product.batch_No)
                  input(type="hidden", name="price", value=product.price)

                  .quantity-wrapper
                    button(type="button", class="btn-decrease", aria-label="Decrease") −
                    input(type="text", name="quantity", value="1", readonly, data-min="1", data-max="50")
                    button(type="button", class="btn-increase", aria-label="Increase") +


                  button.btn.btn-primary.mt-2(type="submit") Add to Cart
      
